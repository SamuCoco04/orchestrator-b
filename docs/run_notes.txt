- Force-openai cross-review path now writes provider artifact and run summary provider line.
- Add-only now uses conservative token-threshold batch constants and per-round shortfall re-request behavior.
- Apply-report missing case now triggers targeted format-retry for FINAL_REQUIREMENTS_JSON + APPLY_REPORT_JSON wrapper.
